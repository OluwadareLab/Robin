export const webPath = `${process.env.REACT_APP_HOST_SERVER}:${process.env.REACT_APP_WEB_PORT_HOST}/robin/`

/** the absulute path (read from env var) to output the result.html file to  */
export const outputPath = `${process.env.ABSULUTE_TEST_OUTPUT_PATH}`;
