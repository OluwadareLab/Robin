A container element that wraps other elements, this does not scale those inside, it just shrinks or expands the container.
```jsx

    <ScalableElement defaultSize={.5}>
        <h1>Scale Me!</h1>
    </ScalableElement>
```