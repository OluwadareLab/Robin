The component for getting a protien referece file from the user

```jsx
import { ReferenceFile } from "../tempTypes/Types";
<ProtienReferenceNameInput
    onFileInputChange={async (e) => {}}
    onNameChange={async (e) => {}}
    onRemove={async (e) => {}}
    file={new ReferenceFile(1)}
/>
```